<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.arthur.project" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".controller.ListarVoo"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".controller.ListaDeVoos"
            android:label="@string/title_activity_lista_de_voos" >
        </activity>
        <activity
            android:name=".controller.DetalheVooActivity"
            android:label="@string/title_activity_detalhe_voo" >
        </activity>
    </application>

</manifest>
